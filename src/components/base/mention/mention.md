Mention is used within GFM and indicates when a user is "@" mentioned. The `href` should link to the user's profile.

### Under the hood

Uses [`BLink`](https://bootstrap-vue.org/docs/components/link) from Vue Boostrap
