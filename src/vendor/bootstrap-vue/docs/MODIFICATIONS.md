This directory has been cleaned up, with all files removed except for the `./assets` folder. The
styles within this folder have been retained because they are required for importing the original
BootstrapVue documentation into Storybook, specifically within the `src/scss/storybook.scss` file.
