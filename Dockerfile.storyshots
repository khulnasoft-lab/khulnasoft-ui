ARG PUPPETEER_VERSION
FROM registry.gitlab.com/gitlab-org/gitlab-ui/puppeteer-v2:${PUPPETEER_VERSION}

ADD package.json yarn.lock ./
RUN yarn --frozen-lockfile --ignore-scripts
ADD . .
RUN yarn install --frozen-lockfile --offline --force
